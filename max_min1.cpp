#include <iostream>
#include <cstdlib>
#include <vector>
#include <algorithm>
#include <iterator>

using namespace std;

int main() {
    cout << "max(5, 3) = " << max(5, 3) << endl;
    cout << "min(5, 3) = " << min(5, 3) << endl;
    exit(EXIT_SUCCESS);
}
